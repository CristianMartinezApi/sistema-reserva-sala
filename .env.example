# Firebase Configuration (Already in firebase-config.js)
# These are public keys - security is handled by Firebase Rules

# Application Settings
APP_NAME=Sistema de Reserva de Sala - PGE-SC
APP_PORT=8080
APP_ENV=production

# Firebase Project Settings (for reference)
# FIREBASE_PROJECT_ID=do-sistema-de-reserva-sala
# FIREBASE_AUTH_DOMAIN=do-sistema-de-reserva-sala.firebaseapp.com

# Timezone
TZ=America/Sao_Paulo

# Rate Limiting (implemented in frontend, consider moving to backend)
MAX_RESERVATIONS_PER_HOUR=5

# Reservation Settings
MIN_ADVANCE_MINUTES=30
MAX_RESERVATION_HOURS=8
WORKING_HOURS_START=06:00
WORKING_HOURS_END=22:00

# Security
# Consider implementing these in Cloud Functions:
# - ENABLE_RATE_LIMITING=true
# - ENABLE_APP_CHECK=true
# - ALLOWED_DOMAINS=pge.sc.gov.br,localhost
